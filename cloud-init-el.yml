#cloud-config
fqdn: XXX_HOSTNAME
users:
  - default
  - name: sysadm
    shell: /usr/bin/bash
    sudo: ALL=(ALL) NOPASSWD:ALL
    ssh-authorized-keys:
      - ssh-ed25519 PUT-YOUR-KEY-HERE

packages:
  - sudo

runcmd:
  - echo AuthenticationMethods publickey >> /etc/ssh/sshd_config
